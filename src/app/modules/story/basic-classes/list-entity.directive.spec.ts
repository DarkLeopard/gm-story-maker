import { ListEntityDirective } from './list-entity.directive';

describe('ListEntityDirective', () => {
  it('should create an instance', () => {
    const directive = new ListEntityDirective();
    expect(directive).toBeTruthy();
  });
});
